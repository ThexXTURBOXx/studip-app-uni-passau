<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="studip_uni_passau.femtopedia.de.unipassaustudip.MensaActivity"
    tools:showIn="@layout/app_bar_mensa">

    <TextView
        android:id="@+id/dateView"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:gravity="center"
        android:textSize="20sp"
        app:layout_constraintEnd_toStartOf="@+id/next_date"
        app:layout_constraintStart_toEndOf="@+id/previous_date" />

    <android.support.v4.widget.SwipeRefreshLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/swiperefresh_mensa"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="36dp"
        app:layout_constraintTop_toBottomOf="@+id/dateView">

        <ExpandableListView
            android:id="@+id/mensacontent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </android.support.v4.widget.SwipeRefreshLayout>

    <Button
        android:id="@+id/previous_date"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="@drawable/ic_keyboard_arrow_left_white_24dp"
        android:onClick="onClickButtonDatePrev" />

    <Button
        android:id="@+id/next_date"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="@drawable/ic_keyboard_arrow_right_white_24dp"
        app:layout_constraintEnd_toEndOf="parent"
        android:onClick="onClickButtonDateNext" />

</android.support.constraint.ConstraintLayout>
